# BitDX Prop Firm frontend environment (template, no secrets)
# Copy to .env.local for local development.

# Base URL of the BitDX Prop Firm backend (see ../PropFirm Backend).
NEXT_PUBLIC_PROPFIRM_API_URL=http://localhost:8090
